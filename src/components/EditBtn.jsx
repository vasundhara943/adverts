import React from "react";
import { Button } from "@mui/material";

const EditBtn = () => {

    const editValues = () => {
        
    }
  return (
    <Button variant="contained" color="primary" onClick={editValues}>
      Edit
    </Button>
  );
};

export default EditBtn;
